---
bump: major
---

**Breaking:** rename the package from `parsil_zig` to `parsil` (the
module name was already `parsil`, so `@import("parsil")` is unchanged
— only the dependency name in `build.zig.zon` is affected). Migration:

```diff
-const parsil = b.dependency("parsil_zig", .{ ... }).module("parsil");
+const parsil = b.dependency("parsil", .{ ... }).module("parsil");
```

The `parsil-zig` name persists only as the GitHub repo URL (preserved
for backward link stability — pre-v1.0 consumers using the existing
`zig fetch --save git+https://github.com/salty-max/parsil-zig` URL
keep working without change).
