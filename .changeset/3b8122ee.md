---
bump: major
---

Add `ok(value, index)` helper for constructing successful `ParseResult(T)` values; T inferred from `value` via `anytype`. Annotate slice expressions explicitly (`const cand: []const u8 = ...`) so inference resolves past Zig's bounded-slice → `*const [N]u8` quirk. Document why parsil-TS's `forward`/`updateState`/`updateResult`/`updateError` don't translate to parsil-zig's split-state design.

**BREAKING**: removes the speculative `Note` struct and `notes: []const Note` field from `ParseError`. Compiler-style secondary diagnostics turned out to be design overreach — no real consumer (Gero asm/lang/LSP) needed them, while they added a separate type and ~30 lines of recursive formatter code. `hint`, `severity`, and `kind` stay; `notes` was the speculative outlier. `formatParseErrorPretty` no longer renders a notes section.
