---
bump: minor
---

Add `ok(value, index)` helper for constructing successful `ParseResult(T)` values. Inferring `T` from `value` via `anytype` — use an explicit `[]const u8` annotation on slice expressions (`const cand: []const u8 = ...`) to guide inference past Zig's bounded-slice → `*const [N]u8` quirk. Document why parsil-TS's `forward`/`updateState`/`updateResult`/`updateError` don't translate to parsil-zig's split-state design.
