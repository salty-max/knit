---
bump: minor
---

Add `inContext(T, label, p)` and `label(T, name, p)` error-context wrappers. `inContext` pushes a label onto `ParseError.context` outer-first (allocating via `state.allocator`); `label` replaces the inner parser's identity with the supplied name. First combinators that exercise the arena allocator at runtime — leak-checked via `std.testing.allocator` in tests.
