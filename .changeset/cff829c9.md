---
bump: major
---

Replace minimal `ParseError` with a rich shape: `parser`, `index`, `message`, optional `expected`/`actual`/`context`. Adds `parseError(...)` factory and `formatParseError(allocator, err)` formatter. Drops the `ParseErrorTag` enum — error identity now lives in the string `parser` field.
