---
bump: minor
---

Add bounded repetition (`atLeast`, `atMost`, `repeatBetween`) under
`parsers/many/` and associative-fold combinators (`chainl1`,
`chainr1`) under `parsers/chain/`. `chainl1` runs in constant
stack with no allocation; `chainr1` collects pairs then folds from
the right.
