---
bump: minor
---

Add Tier C parsil-TS parity gap-fills (closes #120, #121): bounded repetition (`atLeast`, `atMost`, `repeatBetween`) under `parsers/many/` and associative-fold combinators (`chainl1`, `chainr1`) under `parsers/chain/`. `chainl1` runs in constant stack with no allocation; `chainr1` collects pairs then folds from the right.
